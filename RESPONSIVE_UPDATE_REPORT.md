# mi-trust.net レスポンシブ表示最適化 修正報告

更新日: 2026-08-27

## 1. 修正したページ

- `index.html` — トップページ
- `founder.html` — CEOプロフィール詳細ページ
- `about.html` — 会社概要ページ
- `privacy.html` — プライバシーポリシー

ブランドカラー、ロゴ、写真、ページ構成、基本セクション順、コンテンツの主旨は変更していません。

## 2. 修正したCSS・コンポーネント

全4ページの既存インラインCSS末尾に、共通の `Responsive quality optimization` オーバーライドを追加しました。

主な対象:

- `.container` / `.section` / `.page-section`
- `h1` / `h2` / `h3` / `.sec-title` / `.statement` / `.bigline`
- `.hero` / `.hero h1` / `.hero-sub` / `.hero-cta`
- `.subhero`
- `.pain` / `.svc` / `.case` / `.proc`
- `.engagement-card`
- Founderページの `.achievement` / `.capability` / `.timeline-row` / `.edu-card`
- `.btn` / `.ccard` / `.m-cta`
- `.contact` / `.page-contact-band` / `.contact-form`
- `.profile-row` / `.policy-item`
- ヘッダーナビゲーション / モバイルナビ / フッター

主要見出しに `text-wrap: balance`、本文に `text-wrap: pretty` と日本語向け `line-break: strict` を適用しています。

## 3. PC版から変更したスマホ固有の設定

- 768px以下の左右余白を20pxに統一
- Hero見出しを画面幅連動の `clamp()` で再調整
- セクション見出し、Statement、Subhero、Contact見出しをスマホ用スケールへ調整
- Hero CTAは430px以下で縦1列・幅100%に変更
- ボタンの最小高さを48pxにし、タップしやすさを確保
- カード内左右余白を20〜28pxの範囲で最適化
- 1080px以下ではデスクトップ7項目ナビをハンバーガーメニューへ切替
- 1080px以下ではトップHero右端の縦書き装飾コピーを非表示（タブレットでの横はみ出し防止）
- 430px / 390px / 375pxで見出しサイズ・カード余白・CTAを追加微調整
- 390px / 375pxでもページ全体の横スクロールが発生しないよう制御

## 4. 強制改行を削除・変更した箇所

### トップページ

- HeroコピーをPCとスマホで別の自然な改行へ変更
  - PC: 「経営課題を、変革テーマに。」／「変革テーマを、現場の実行へ。」の2行
  - スマホ: 意味単位の4行
- 「いま多くの企業で起きているのは…」の `<br>` を `pc-only` 化
- 「経営・マネジメント・現場を、1つの設計としてつなぐ」の `<br>` を `pc-only` 化
- 「コンサルでも、研修会社でも、AI導入会社でもない」の `<br>` を `pc-only` 化

### Aboutページ

- 「経営課題を、変革テーマに。変革テーマを、現場の実行へ。」のPC用改行と自然折返しを分離

### Founderページ

- 「経営の論点を、組織が動く言葉へ」のPC用改行を `pc-only` 化
- 5つの価値提供カード内タイトルのPC用 `<br>` を `pc-only` 化し、スマホでは自然折返しへ変更

## 5. `nowrap` を設定した箇所

横スクロールを起こさない短い重要語句に限定して設定しています。

- トップHero: 「経営課題を、」「変革テーマに。」「変革テーマを、」「現場の実行へ。」
- 「戦略がない」「実行が変わらない」
- 「経営・マネジメント・現場」
- 「AIエージェント」
- 「AI導入会社」
- 「MIトラスト合同会社」
- Aboutページの主要Heroフレーズ
- Founderページの「経営の論点を、」「組織が動く言葉」

メールアドレスや長いURLは `nowrap` にせず、必要時に安全に折り返せるようにしています。

## 6. 各ブレークポイントでの確認結果

Chromiumで4ページを実レンダリングし、以下6幅 × 4ページ = 24パターンを検査しました。

| 幅 | トップ | About | Founder | Privacy |
|---|---|---|---|---|
| 1440px | OK | OK | OK | OK |
| 1024px | OK | OK | OK | OK |
| 768px | OK | OK | OK | OK |
| 430px | OK | OK | OK | OK |
| 390px | OK | OK | OK | OK |
| 375px | OK | OK | OK | OK |

確認項目:

- ページ全体の横スクロール
- viewport外へはみ出す要素
- ボタン内部の横方向オーバーフロー
- 5行を超えて細切れになる見出し

すべて最終確認時は問題なしです。

## 7. 横スクロール・レイアウト崩れ確認

最初の検査ではトップページの `.hero-vert` が1024px / 768pxで右端に約6pxはみ出していました。

1080px以下ではこの装飾要素のみ非表示にした後、再検査を実施し、全24パターンで:

- `scrollWidth - clientWidth = 0`
- viewport外の要素 = 0
- ボタンの横オーバーフロー = 0
- 5行超の見出し = 0

を確認しています。
